{
  "name": "tcc-ebac-ui",
  "version": "1.0.0",
  "description": "Testes de UI com Cypress - EBAC Shop",
  "scripts": {
    "test": "cypress run --headless --browser chrome",
    "test:open": "cypress open"
  },
  "devDependencies": {
    "cypress": "^13.0.0"
  }
}